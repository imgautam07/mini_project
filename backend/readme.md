cmd to start surreal DB


    surreal start -u root -p root rocksdb://data

cmd to start backend server
    
    cargo run


- API
    - login
    `email: String , password: String` returns a jwt token
    - signup 
    `email: String , password: String` returns a jwt token